@{
    Layout = null;
}
@{
}
@* @{
    ViewData["Title"] = "Dashboard";
} *@

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <link rel="stylesheet" href="~/css/Log1.css">
    <style>
        .btn {
            justify-content: space-between;
            align-items: center;
            margin: 5px auto;
        }

        .btn {
            background: #f8f9fa; /* Light background */
            border-bottom: 2px solid #ddd; /* Matches the navbar style */
            padding: 10px 20px;
        }

            .btn ul {
                display: flex;
                gap: 20px;
                list-style-type: none; /* Removes bullet points */
                margin-left: 795px;
                padding: 0;
            }

                .btn ul li {
                    margin: 0;
                }

                    .btn ul li a {
                        text-decoration: none;
                        font-size: 14px;
                        color: #333;
                        padding: 5px 10px;
                        border-radius: 5px;
                        transition: background-color 0.3s, color 0.3s;
                    }

                        .btn ul li a:hover {
                            background-color: #ddd;
                            color: #000;
                        }

/* 
        .btn input[type="button"] {
            margin-top: 5px;
            padding: 10px;
            width: 15px;
            border: none;
            background-color: #e74c3c;
            color: white;
            font-size: 1rem;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            float:right;
        }

            .btn input[type="button"]:hover {
                background-color: #c0392b;
            } */
    </style>
</head>
<body>
    <div>
        <img src="~/images/college.png" alt="Logo" class="Logo" height="40px" width="60px" style="position:absolute; top:25px; left:25px;" />
    </div>
    <div class="btn">
        <ul>
            @* <input type="button" value="Logout" onclick="logout()" /> *@
            <li><a conasptroller="Admin" asp-action="Enquiry">Enquriy </a></li>
            </li>
            <li><a conasptroller="Admin" asp-action="AddNotice">Notice</a></li>
            <li><a conasptroller="Admin" asp-action="AdminAction">Applications </a></li>
            <li>
                <a conasptroller="Admin" asp-action="RegUsers">Reg Users </a>
            </li>

            <li><a conasptroller="Admin" asp-action="AdminCourses">Course</a></li>

        </ul>

    </div>
    <div class="dashboard-container">

        <div class="sidebar">
            <h2>Dashboard</h2>
            <ul>
                <li><a conasptroller="Admin" asp-action="AdminCourses">Course</a></li>
                <li>
                    <a conasptroller="Admin" asp-action="RegUsers">Reg Users </a>
                </li>
                <li><a conasptroller="Admin" asp-action="AdminAction">Applications </a></li>
                <li><a conasptroller="Admin" asp-action="AddNotice">Notice</a></li>
                <li><a conasptroller ="Admin" asp-action="Enquiry">Enquriy </a></li>        
                <input type="button" value="Logout" onclick="logout()" />
            </ul>
        </div>
        
       
        <div class="main-content">
            <header>
                <h1>Welcome  @ViewBag.Username</h1>
            </header>

           
            <script>
                function logout() {
                window.location.href = '@Url.Action("Login", "User")';
                }
            </script>
</body>
</html>
